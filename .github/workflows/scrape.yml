name: hidden-bids

on:
  schedule:
    - cron: '0 23 * * *'      # 매일 08:00 KST (UTC 23:00) — Apps Script 08:30 수집보다 먼저
  workflow_dispatch:           # Actions 탭에서 'Run workflow' 버튼으로 바로 실행

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install
      - run: npx playwright install --with-deps chromium
      - run: node scrape.mjs
      - name: 첨부파일 받기 (우리카드·하나금융 — 실제 브라우저가 있어야 받아지는 곳)
        run: node att.mjs || echo "첨부 받기 일부 실패"
      - name: 결과 커밋
        run: |
          git config user.name  "hidden-bids-bot"
          git config user.email "actions@users.noreply.github.com"
          git add -A data/
          git commit -m "hidden bids $(date -u +%Y-%m-%dT%H:%MZ)" || echo "변경 없음"
          git push
